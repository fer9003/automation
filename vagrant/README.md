### Vagrant commands for workloads

`vagrant init`
`vagant validate`
`vagrant up`
`vagrant status`
`vagrant global-status`
`vagrant ssh-config` 
`vagrant reload`    
`vagrant suspend`
`vagrant halt`     
`vagrant destroy` 
`vagrant provision` 